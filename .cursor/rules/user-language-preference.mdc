---
description: 사용자가 금지한 한국어 표현을 모든 응답과 작성물에서 제외
alwaysApply: true
---

# 사용자 언어 선호

- 유니코드 코드 포인트 `U+CD95`에 해당하는 한글 한 글자 단어를 사용하지 않는다.
- 대화, 기획 문서, 코드 주석, 커밋 메시지 등 에이전트가 새로 작성하는 모든 텍스트에 적용한다.
- 문맥에 따라 `항목`, `요소`, `차원`, `기준`, `방향`, `분류` 같은 표현으로 바꿔 쓴다.
