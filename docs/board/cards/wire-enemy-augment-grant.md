# EnemyAugmentGrantComponent 연결

## 현황

`EnemyAugmentGrantComponent.grant()` API는 있으나 씬에서 사용하지 않는다. 오퍼 UI를 거치지 않는 드롭/픽업형 적 강화에 쓸 수 있다.

## 아이디어

- 특정 적 처치 시 grant
- 아이템 픽업 시 grant
- 디버그/치트 버튼

## 참고

- `components/enemy_augment_grant_component.gd`
