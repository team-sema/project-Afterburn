# Feature: 증강 모듈 슬롯

## 목적

플레이어 **시설 효과** 증강을 범용 슬롯에 장착한다. 기존 함선 부위 구분은 module tag와 아이콘으로 유지한다. 무기 획득·전용 모듈은 시설 슬롯을 쓰지 않는다. 상세 전환 규칙은 [unified-module-slots](unified-module-slots.md)가 정본이다.

## 규칙

1. `FACILITY_EFFECT` 오퍼만 module tag를 가지며 범용 슬롯 하나를 차지한다.
2. 동일 전투 스탯을 `STAT_MULTIPLIER` 등으로 **이중 적용하지 않는다.** (플레이어 오퍼 풀에 스탯 모듈 없음)
3. `WEAPON_ACQUIRE` / `WEAPON_TRAIT`는 시설 슬롯을 소모하지 않는다. 모듈 레벨은 `PlayerWeaponLoadout`에 런 동안 반영된다.
4. 범용 슬롯은 5개로 시작한다.
5. 범용 슬롯 용량은 오퍼를 소비해 1개씩 늘릴 수 있고 최대 15개다.
6. 빈 슬롯이 있으면 선택한 모듈을 첫 빈 슬롯에 설치한다.
7. 범용 풀이 가득 찼으면 교체 모달에서 tag와 무관하게 기존 모듈 하나를 고른다. 취소하면 오퍼 선택으로 돌아간다.
8. 교체된 모듈의 시설 효과는 즉시 제거된다.
9. 동일 모듈의 중복 설치와 효과 중첩을 허용한다 (시설 `module_count_values` 곡선).
10. 적 증강 오퍼는 슬롯 없이 누적 레지스트리를 유지한다.

## 대상 부위

| 부위 | 효과 모듈 |
|---|---|
| 무기실 | 집속 조준기 (공통 공격력) |
| 격납고 | 격납고 확장 (효과 미정) |
| 엔진 | 추력 편향기 (이동속도) |
| 선체 | 반응 장갑 |
| 레이더 | 광역 탐지기 |
| 실드 | 실드 축전기 |

## 오퍼 UI

```text
증강 1    증강 2    증강 3
--------------------------------
함선 부위와 현재 모듈 슬롯
```

- 카드 포커스·호버는 같은 primary tag가 장착된 범용 슬롯 외곽선을 하이라이트한다. 무기 전용 카드는 시설 tag 하이라이트를 강제하지 않는다.
- 6개 시설 칩은 표시하지 않고, tag는 5×3 범용 육각 슬롯의 임시 아이콘과 데이터로 유지한다.
- 단일 `범용 슬롯 +1` 버튼에 포커스·호버하면 다음 육각 칸이 점멸하고, 선택 시 용량을 확장한다.
- 전투 중 `ShipPanel`은 읽기 전용이다.

## 구현

- 상태: `player_augment_registry.gd`, `resources/player_augments/player_augment_module_state.gd`
- 효과: `components/ship_facility_applier.gd` (+ 엔진 이속은 `PlayerAugmentApplier`가 시설 배율만 합성)
- 흐름: `augment_offer_controller.gd`
- UI: `menus/augment_selection_overlay.*`, `menus/augment_module_swap_overlay.*`, `menus/ship_panel.*`
- 목록 정본: `docs/spec/augments.md`, `docs/spec/player.md`

## Acceptance Criteria

- [x] 시설 모듈만 범용 시설 슬롯을 사용한다
- [x] 플레이어 오퍼에 스탯 모듈 이중 경로 없음
- [x] 무기 Kind는 슬롯 없이 로드아웃에 적용된다
- [x] 범용 빈 슬롯 설치·가득 찬 전체 슬롯 교체·취소가 동작한다
- [x] 범용 슬롯 확장이 동작한다
- [x] 적 증강 오퍼는 슬롯 없이 유지된다

## 변경 이력

| 날짜 | 변경 |
|---|---|
| 2026-08-01 | 시설 레벨과 누적 증강을 부위별 장착 모듈로 통합 |
| 2026-08-05 | 무기 Kind는 슬롯 비소모로 정합 |
| 2026-08-05 | 스탯 모듈 제거 · 시설 효과만 부위 슬롯 담당 |
| 2026-08-06 | 부위별 슬롯을 시작 5칸·최대 15칸의 5×3 육각 벌집으로 통합, 부위는 tag로 유지 |
