# 증강 TODO (아이디어)

구현 후보·밸런스 메모를 쌓는 **아이디어 보드**다.  
확정된 동작은 `/feature`로 `docs/design/systems/<slug>.md` + Task를 만들고, 들어간 뒤에는 [`docs/spec/augments.md`](../spec/augments.md)가 정본이다.

현재 오퍼 풀(플레이어 **54** / 적 **6**)은 [`docs/spec/augments.md`](../spec/augments.md)·`gameplay.tscn`이 정본이다. 여기 항목은 **아직 풀에 없는 후보**를 우선 적는다.

## 상태

| 태그 | 의미 |
|------|------|
| `idea` | 막연한 후보 |
| `maybe` | 재미는 있는데 수치·구현 비용 미정 |
| `ready` | 방향 합의됨 → 곧 feature 후보 |
| `done` | 풀·스펙에 반영됨 (이력만 남기거나 아래로 내림) |
| `dropped` | 안 하기로 함 |

행을 추가할 때 **대상(플레이어/적) · 한 줄 효과 · 상태 · 메모**만 적어도 된다.

---

## 플레이어

| 아이디어 | 상태 | 메모 |
|----------|------|------|
| **격납고 → 동력로** | `done` | `power-core-facility`·`facility-weapon-modules`로 반영. 아래 「동력로」 절은 이력 |
| 피격 범위(Hurtbox) 감소 | `idea` | 회피 여유↑. 시각 실루엣과 히트박스 불일치 주의. 부위 후보: 선체/실드? |
| 무기 관통 수 증가 | `idea` | 부위: 무기실. 투사체/샷건 등에 pierce +N. 레이저·빔은 제외 또는 별 규칙 필요 |
| 투사체형 무기에 호밍 추가 | `idea` | 부위: 무기실(또는 동력로). 직선 탄에 약한 유도. `aux_homing_missile`과 중첩·차별 정의 필요. 빔·궤도 방벽 제외 |

---

## 동력로 (반영 완료 · 초안 이력)

**구현됨** — 정본은 [`docs/spec/augments.md`](../spec/augments.md) 「시설 효과 모듈 13종」. 아래는 당시 초안이며 실제 출하 수치와 다르다.

역할 분담: **무기실** = 지속 기본 화력(집속 조준기 상시 배율) · **동력로** = 주기적 과부하(버프 윈도우).

### 초안 대비 실제 차이

| 항목 | 초안 | 실제 |
|------|------|------|
| 슬롯 키 | `hangar` → `reactor` rename | **`hangar` 유지** (호환), UI 표시명만 동력로 |
| 과충전 반응로 | 20초마다 5초 ×1.3 | `facility_hangar` — ×**1.4** · 5초 · 주기 20초 |
| 비상 전력망 | 피격 시(**실드 흡수 포함**) 5초 ×1.2 · CD 15초 | `facility_reactor_emergency` 비상 출력 장치 — ×**1.3** · 5초 · CD 15초 · **선체 피격만** |

---

## 적

| 아이디어 | 상태 | 메모 |
|----------|------|------|
| 파괴 시 짧은 딜레이 후 폭발 | `idea` | 시체/잔해 연출 → AOE. Bomb 근접 자폭과 차별(전 적? 일부 타입만?). 플레이어 탄으로 킬해도 터지는지 정해야 함 |
| 보복 프로토콜 (`enemy_counter_shot_on_hit`) | `maybe` | 리소스는 있음 · **현재 적 풀 미연결**. 풀 재투입만으로 가능할지 확인 |

---

## 시스템 · UX (증강 주변)

| 아이디어 | 상태 | 메모 |
|----------|------|------|
| 풀 가중치 / 등장 확률 | `idea` | 스펙 gaps: 균등 셔플이라 카드 늘수록 개별 확률↓ |
| **실드 재생 + 충전 UI** | `done` | `shield-regen` · `shield-base-one` 반영 |
| **동력로 (격납고 대체)** | `done` | `power-core-facility` · `facility-weapon-modules` 반영 |

---

## 실드 재생 (반영 완료 · 초안 이력)

**구현됨** — 정본은 [`docs/spec/player.md`](../spec/player.md)·[`docs/design/systems/shield-regen.md`](systems/shield-regen.md). 아래는 당시 초안이다.

현재 실드가 최대 미만이면 **한 칸씩** 회복한다. 시설로 최대치만 늘리는 것과 별개. **시작 실드 1** (`shield-base-one`).

### 규칙

1. **실드가 깎이는 즉시**(또는 현재 < 최대인 동안) 충전 게이지가 차기 시작한다. 별도 무피격 대기 없음.
2. 잔여 실드 게이지 **아래**에 작은 **충전 게이지**가 천천히 찬다 (충전 초는 `@export` / 데이터).
3. 충전 게이지가 꽉 차면 현재 실드 **+1** (최대 이하) · 게이지 리셋 후, 아직 최대 미만이면 바로 다음 칸 충전 재개.
4. 피격(실드 흡수·선체 피격 모두) 시 **충전 게이지만 리셋**하고, 실드가 여전히 최대 미만이면 **즉시 다시 차기 시작**.
5. **한 칸이 완전히 채워지기 전에는 그 충전분으로 방어되지 않음.** 이미 보유한 실드만 버퍼 HP. 피해는 실드 → 선체 순으로 깎인다 (전체 이벤트 차단 게이트 없음).
6. 현재 = 최대이면 충전 게이지는 0·숨김.

### UI

- `ShipStatusHud` 실드 바 아래 세컨더리 ProgressBar(또는 동등)로 충전률 0~1 표시.

### 메모

- `restore_shield(1)`를 충전 완료 시에 호출하는 경로가 자연스러움.
- 비상 전력망(동력로)은 실드 흡수 피격에도 발동 — 그 피격도 충전 게이지를 리셋한 뒤 즉시 재충전.

---

## 메모 규칙

- **플레이어** 후보는 가능하면 `facility_id`(무기실·동력로(슬롯 키 `hangar`)·엔진·선체·레이더·실드)를 적어 둔다.
- **적** 후보는 스탯(`HEALTH` / `MOVE_SPEED` / `ACTION_RATE`)인지, behavior 컴포넌트인지 구분한다.
- 수치 초안이 있으면 메모에 (예: Hurtbox scale 0.85, 폭발 딜레이 0.4s).
- 구현 착수 시 이 표의 상태를 `ready` → feature slug를 메모에 남기고, 머지 후 `done` 처리.

---

## 변경 이력

| 날짜 | 변경 |
|---|---|
| 2026-08-06 | 풀 수 22/3 → **54/6** 정정 · 동력로·실드 재생 절을 `done`(초안 이력)으로 정리 · 초안 대비 실제 수치 차이 명시 |
| 2026-08-06 | 실드 재생: **깎이면 즉시** 소형 게이지 충전 (무피격 대기 없음) · 피격 시 게이지 리셋 후 즉시 재충전 |
| 2026-08-06 | **실드 재생** 초안 · 격납고 대체 **동력로** |
| 2026-08-02 | 관통 수 · 투사체 호밍 후보 추가 |
| 2026-08-02 | 초안 · 피격범위·파괴 후 폭발 예시 |
